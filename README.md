## Chem 7870 Project

Model and plot the band behavior of a metal tip-semiconductor surface system, whose capacitance varies according to distance between the tip and surface.
Install using:

```
pip install -e .
```

in the root directory of this repository.  Once the code is installed, to test your code you can run the following command in the root directory of this repository:

```
pytest tests
```

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
authors = [
   { name = "Felix Mo", email = "fm466@cornell.edu"},
]
name = "chem_7870_proj_fm466"
version = "0.0.1"
dependencies = [
   "numpy",
   "matplotlib",
   "pytest",
   "scipy",
]
